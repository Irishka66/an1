(function() {
    "use strict";

    let first = {
        bindings: {
        },
        controller: function () {
        },
        templateUrl: "templates/first.html",
    };

    angular.module("app")
        .component("first", first);
})();

